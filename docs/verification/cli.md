# cli 测试用例

## 启动终端对话

- **场景**：在终端执行 `closeclaw chat` → 在同一终端进程中进入持续对话，可连续多轮收发消息（此功能依赖 daemon 已运行）
- **来源**：需求 §F1
- **LLM 方式**：fake
- **阶梯级**：L1
- **状态**：待实现

## 指定 Agent 独立对话

- **场景**：用 `--agent-id` 指定目标 Agent 启动对话 → 与指定 Agent 交互；对不同 Agent 分别启动对话时，各自会话保持独立、互不干扰
- **来源**：需求 §F1
- **LLM 方式**：fake
- **阶梯级**：L1
- **状态**：待实现

## 终端斜杠指令体系与 IM 一致

- **场景**：在终端对话中发送斜杠指令（如 `/help`）→ 指令被拦截并执行，行为与 IM 渠道完全一致
- **来源**：需求 §F1
- **LLM 方式**：fake
- **阶梯级**：L3
- **状态**：待实现

## 终端调用者默认为 Owner

- **场景**：在终端对话中执行操作 → 调用者身份默认为 Owner，无需额外鉴权
- **来源**：需求 §F1
- **LLM 方式**：fake
- **阶梯级**：L1
- **状态**：待实现

## 结束当前对话

- **场景**：在终端对话中输入 `/stop` → 结束当前对话
- **来源**：需求 §F1
- **LLM 方式**：fake
- **阶梯级**：L3
- **状态**：待实现

## Markdown 转 ANSI 渲染

- **场景**：Agent 回复包含 markdown 格式标记（标题、粗体、斜体、引用、链接、分割线）→ 渲染为对应 ANSI 样式；列表（`- `/`* ` 无序、`1. ` 有序）保留原始 markdown 标记原样输出
- **来源**：需求 §F2
- **LLM 方式**：fake
- **阶梯级**：L1
- **状态**：待实现

## 代码块语法高亮与行号

- **场景**：Agent 回复包含带语言标注的代码块 → 按语言注入语法高亮与行号；未知语言回退为纯文本输出
- **来源**：需求 §F2
- **LLM 方式**：fake
- **阶梯级**：L1
- **状态**：待实现

## Thinking 折叠展示

- **场景**：Agent 产生思考过程 → 以折叠块形式展示，带首尾边界标记，不打断对话阅读流
- **来源**：需求 §F2
- **LLM 方式**：fake
- **阶梯级**：L1
- **状态**：待实现

## 工具调用与结果展示

- **场景**：Agent 调用工具 → 展示工具名称与参数；工具结果以截断方式展示
- **来源**：需求 §F2
- **LLM 方式**：fake
- **阶梯级**：L2
- **状态**：待实现

## 媒体占位符

- **场景**：对话中出现终端不支持的媒体类型（图片、音频、文件）→ 展示为带文件名的占位符，不输出二进制内容
- **来源**：需求 §F2
- **LLM 方式**：fake
- **阶梯级**：L2
- **状态**：待实现

## DSL 交互元素降级渲染

- **场景**：对话输出含按钮、选择器等交互元素 → 终端渲染为纯文本提示行（如 `[Button: 标签 (action: xxx)]`），不渲染交互控件
- **来源**：需求 §F2
- **LLM 方式**：fake
- **阶梯级**：L2
- **状态**：待实现

## 终端宽度适配

- **场景**：某块输出超过终端可用宽度 → 截断并标注，不产生溢出
- **来源**：需求 §F2
- **LLM 方式**：fake
- **阶梯级**：L1
- **状态**：待实现

## 流式输出逐行显示

- **场景**：LLM 流式输出 → 内容逐行增量显示，不等待完整回复
- **来源**：需求 §F2
- **LLM 方式**：fake
- **阶梯级**：L1
- **状态**：待实现

## 启动 daemon

- **场景**：执行 `closeclaw run` → 启动 daemon 进程
- **来源**：需求 §F3
- **LLM 方式**：fake
- **阶梯级**：L0
- **状态**：待实现

## 停止 daemon

- **场景**：执行 `closeclaw stop` → 停止 daemon 进程
- **来源**：需求 §F3
- **LLM 方式**：fake
- **阶梯级**：L0
- **状态**：待实现

## daemon 未运行时 stop 不报错

- **场景**：daemon 未运行时执行 `closeclaw stop` → 不报错，正常退出
- **来源**：需求 §F3
- **LLM 方式**：fake
- **阶梯级**：L0
- **状态**：待实现

## daemon 已运行时 run 拒绝新实例

- **场景**：daemon 已运行时执行 `closeclaw run` → 拒绝启动新实例
- **来源**：需求 §F3
- **LLM 方式**：fake
- **阶梯级**：L0
- **状态**：待实现

## 交互式配置向导

- **场景**：首次配置时启动向导 → 以引导式问答完成配置，完成后自动写入配置文件
- **来源**：需求 §F4
- **LLM 方式**：fake
- **阶梯级**：L1
- **状态**：待实现

## Provider 选择自动应用推荐协议

- **场景**：向导中选择 LLM Provider → 自动应用该 Provider 的推荐协议，用户无需了解协议差异
- **来源**：需求 §F4
- **LLM 方式**：fake
- **阶梯级**：L1
- **状态**：待实现

## 一次选择多个模型

- **场景**：向导中 → 可一次选择多个可用模型
- **来源**：需求 §F4
- **LLM 方式**：fake
- **阶梯级**：L1
- **状态**：待实现

## 凭据输入不回显

- **场景**：向导中输入凭据 → 字符不回显，输入完成后显示确认行让用户确认操作成功
- **来源**：需求 §F4
- **LLM 方式**：fake
- **阶梯级**：L1
- **状态**：待实现

## 模型列表自动获取与失败回退

- **场景**：向导中 → 自动获取可用模型列表；总体获取失败时自动回退到内置模型清单并显示提示；网络超时、认证失败、凭据无效等错误允许用户重试
- **来源**：需求 §F4
- **LLM 方式**：fake
- **阶梯级**：L1
- **状态**：待实现

## Ctrl+C 安全退出向导

- **场景**：向导进行中按 Ctrl+C → 安全退出，不残留不完整配置文件
- **来源**：需求 §F4
- **LLM 方式**：fake
- **阶梯级**：L1
- **状态**：待实现

## 配置校验

- **场景**：对现有配置文件执行校验 → 校验格式合法性并输出校验结果
- **来源**：需求 §F4
- **LLM 方式**：fake
- **阶梯级**：L1
- **状态**：待实现

## 查询与管理 Agent 实例

- **场景**：通过 CLI 查询/列出已配置的 Agent 实例 → 返回 Agent 实例信息（此功能依赖 daemon 已运行）
- **来源**：需求 §F5
- **LLM 方式**：fake
- **阶梯级**：L1
- **状态**：待实现

## 查询 Skill 列表

- **场景**：通过 CLI 查询 Skill 列表 → 返回当前已加载的 Skill 清单（此功能依赖 daemon 已运行）
- **来源**：需求 §F6
- **LLM 方式**：fake
- **阶梯级**：L2
- **状态**：待实现

## 触发重新扫描加载新 Skill

- **场景**：通过 CLI 触发 daemon 重新扫描技能目录 → 新加入的 Skill 被加载
- **来源**：需求 §F6
- **LLM 方式**：fake
- **阶梯级**：L2
- **状态**：待实现

## 校验权限规则语法

- **场景**：通过 CLI 校验权限规则 → 输出规则语法校验结果
- **来源**：需求 §F7
- **LLM 方式**：fake
- **阶梯级**：L4
- **状态**：待实现

## 列出已有权限规则

- **场景**：通过 CLI 列出已有权限规则 → 返回当前已有规则列表
- **来源**：需求 §F7
- **LLM 方式**：fake
- **阶梯级**：L4
- **状态**：待实现
