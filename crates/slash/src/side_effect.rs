//! Execution context for slash command side effects.
//!
//! `SideEffectContext` and `SlashResult::execute()` now live in
//! `closeclaw_common::slash_router` — the single source of truth for
//! these types.
//!
//! This module is retained for backward compatibility but no longer
//! contains type definitions or execute logic.
