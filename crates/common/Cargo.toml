[package]
name = "closeclaw-common"
version = "0.1.0"
edition = "2021"
description = "Shared types and utilities for CloseClaw"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
