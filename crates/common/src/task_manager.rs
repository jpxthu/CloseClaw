//! Task manager types and trait have been migrated to `closeclaw-tasks`.
//!
//! This module is intentionally empty. The canonical definitions of
//! `TaskManager`, `TaskState`, `BackgroundTask`, and `BackgroundTaskError`
//! now live in `closeclaw-tasks`. Consumers should import them from there.
